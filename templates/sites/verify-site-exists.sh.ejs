#!/bin/bash

# The site you are searching for
SEARCH_SITE="<%= siteName %>"

# Run the command and search for the site
if ! list-all-nginx-sites | grep -q "$SEARCH_SITE"; then
echo "Error: Site $SEARCH_SITE not found in the list of available sites."
exit 1
fi

echo "Site $SEARCH_SITE found."
