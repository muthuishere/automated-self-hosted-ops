kubectl create secret generic <%= secretapp %> --from-literal=<%= secretkey %>='<%= secretval %>'

kubectl delete secret <secret-name>
    kubectl create secret generic <secret-name> --from-literal=key1=value1 --from-literal=key2=value2
